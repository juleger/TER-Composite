# Configuration pour test de traction
# Force distribuée sur bord droit, encastrement à gauche

test_type = traction

# Fichier de maillage
mesh_file = mesh/rectangle1.msh

# Matériau: Acier
E = 200.0e9
nu = 0.3
rho = 7850.0

# Force totale appliquée (en Newtons)
F = 1e5

# Solveur
precond = ic
tol = 1e-8
maxIter = 2000

# Série de maillages pour convergence
mesh_base_dir = mesh/square
mesh_prefix = square_lc
mesh_lc = 0.1,0.05,0.025,0.0125,0.00625
