# Configuration pour test de traction
# Force distribuée sur bord droit, encastrement à gauche

test_type = traction

# Fichier de maillage
mesh_file = mesh/rectangle1.msh

# Matériau: Acier
Young_modulus = 200.0e9
Poisson_ratio = 0.3
density = 7850.0

# Force totale appliquée (en Newtons)
force_value = 1000.0

# Fichiers de sortie
output_dir = results
output_prefix = traction
